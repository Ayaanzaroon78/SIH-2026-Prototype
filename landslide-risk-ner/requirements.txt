# Root-level requirements.txt for Vercel's Python serverless runtime.
# Vercel looks for requirements.txt at the project root (or alongside api/index.py).
# This mirrors backend/requirements.txt but pins packages for serverless compatibility.

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.0
alembic>=1.13.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# PostgreSQL drivers (used when DATABASE_URL is a postgres:// connection string)
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# ML stack
xgboost>=2.0.0
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0
numpy>=1.26.0

# Utilities
python-dotenv>=1.0.1
apscheduler>=3.10.4
mangum>=0.17.0
